# HydraTechnology backend — core runtime deps.
#
# Use lower-bound (>=), NOT hard (==) pins for pydantic: pip then picks a version
# that has a prebuilt wheel for the local interpreter, which avoids the Rust
# (pydantic-core) source build on ALL Python versions — including 3.13 and 3.14.
# A hard pin like pydantic==2.10.6 has no wheel on Python 3.14 and forces a Rust
# build that fails. To be safe on any machine, install with:
#     pip install --only-binary=:all: -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.34
SQLAlchemy>=2.0
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0
httpx>=0.28
openpyxl>=3.1
fpdf2>=2.8

# --- Optional (PostgreSQL in production / docker-compose) ---
# psycopg[binary]>=3.2
# Default DB is SQLite (zero-setup). For PostgreSQL set DATABASE_URL, e.g.
#   DATABASE_URL=postgresql+psycopg://hydra:hydra@localhost:5432/hydra
